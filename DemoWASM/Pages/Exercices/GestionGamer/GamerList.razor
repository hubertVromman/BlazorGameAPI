@page "/exo2"
@using DemoWASM.Models
<h3>GamerList</h3>

<div class="row">
    <div class="col-5">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Pseudo</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (Gamer g in Liste)
                {
                    <tr>
                        <td>@g.Pseudo</td>
                        <td class="btn-group-sm">
                            <button class="btn btn-info btn-sm" @onclick="@(() => SelectDetail(g.Id))">Detail</button>
                            <button class="btn btn-warning btn-sm" @onclick="@(() => SelectUpdate(g.Id))">Update</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col-6">
        <div>
            <GamerCreate NotifyNewGamer="LoadData"/>
        </div>
        @if(SelectedDetailId > 0)
        {
            <GamerDetail Id="SelectedDetailId" />
        }
        @if (SelectedUpdateId > 0)
        {
            <GamerUpdate Id="SelectedUpdateId" NotifyUpdatedGamer="LoadData" />
        }
    </div>
</div>